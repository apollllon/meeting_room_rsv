<% provide(:button_text, '編集を保存') %>
<h1><%= @room.name %> の編集</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render "form"%>
    <%= button_to "消去", @room, method: :delete, data: {confirm: "本当に消去しますか？"} %>
  </div>
</div>